# React Router Basics

A minimal React application demonstrating **basic routing** using React Router DOM, including **dynamic routes**.

## Features

- Multiple pages: Home, About, User
- Client-side navigation without page refresh
- Dynamic route example: `/user/:id`

## Installation

1. Clone the repository:

```bash
git clone https://github.com/MilosMicun/react-router-basics.git
cd react-router-basics
Install dependencies:

bash
Copy code
npm install
Start the development server:

bash
Copy code
npm run dev
Open your browser at http://localhost:5173

Usage
Navigate between pages using the links at the top: Home | About | User 42

Visit a dynamic user route by changing the URL, e.g., /user/123

Tech Stack
React 18

Vite

React Router DOM v6

Author
Milos Micun